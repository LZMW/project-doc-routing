# <项目名> 阶段判断

> 最后更新：<YYYY-MM-DD>
> 用途：记录 `<项目名>` 当前阶段结论与优先级判断。
> 只回答：当前在哪个阶段、为什么这样判断、下一步先推什么。
> 不负责：现状快照、详细未来设计、过程流水。

---

## 1. 当前阶段一句话结论

当前 `<项目名>` 处于：`<阶段名称>`

一句话判断：

`<为什么当前是这个阶段>`

---

## 2. 阶段判断依据

- `<依据1：哪些主路径已接通>`
- `<依据2：哪些关键缺口仍在>`
- `<依据3：为什么这会影响优先级>`

---

## 3. 当前活跃主线

当前正在推进：

- `<主线1>`
- `<主线2>`
- `<主线3>`

当前不应抢主位：

- `<暂不优先方向1>`
- `<暂不优先方向2>`

---

## 4. 下一步链路

建议按下面顺序推进：

1. `<下一步1>`
2. `<下一步2>`
3. `<下一步3>`

每一步之所以排在前面，是因为：

- `<原因1>`
- `<原因2>`

---

## 5. 当前不该误写的点

- 不要把 `<方向1>` 写成已实现。
- 不要因为 `<局部已存在能力>` 就把 `<整层能力>` 写成已完成。
- 不要让过程判断替代阶段结论。

---

## 6. 更新触发条件

- 当前优先级变化时更新本文件
- 当前阶段切换时更新本文件
- 如果只是过程波动，不回写这里，统一放进 `.worklog/CURRENT.txt`
