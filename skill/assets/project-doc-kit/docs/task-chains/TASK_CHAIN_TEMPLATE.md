# <编号> <任务链名称>

> 最后更新：<YYYY-MM-DD>
> 角色：未来任务链文档
> 只写：目标、依赖、边界、完成定义
> 不写：现状快照、过程流水、临时讨论

---

## 1. 目标

这条链要解决的是：

- `<目标1>`
- `<目标2>`

---

## 2. 依赖

开始这条链前，至少需要：

- `<依赖1>`
- `<依赖2>`

---

## 3. 边界

这条链负责：

- `<负责内容1>`
- `<负责内容2>`

这条链不负责：

- `<不负责内容1>`
- `<不负责内容2>`

---

## 4. 完成定义

只有同时满足下面条件，才算这条链完成：

- `<完成条件1>`
- `<完成条件2>`
- `<完成条件3>`

---

## 5. 验收方式

建议至少通过下面方式验收：

- `<验收方式1>`
- `<验收方式2>`
