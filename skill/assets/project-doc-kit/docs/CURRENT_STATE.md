# <项目名> 当前现状

> 最后更新：<YYYY-MM-DD>
> 用途：记录 `<项目名>` 当前真实现状。
> 只回答：现在真实有什么、没有什么、从哪里校验。
> 不负责：阶段优先级判断、未来路线设计、过程流水。

---

## 1. 权威顺序

如果文档之间出现冲突，统一按下面顺序理解：

1. 真实代码与真实 git 状态
2. 本文件
3. 各子系统实现事实文档
4. 各子系统局部手册

---

## 2. 当前真实已有能力

### 2.1 主路径

当前真实存在：

- `<主入口A>`
- `<主入口B>`
- `<主对象 / 主流程 / 主界面>`

理解方式必须固定为：

- 这表示哪些主路径已经进入实现态
- 这不等于整个产品蓝图已经做完
- 不要因为某几个入口存在，就把整层能力写成已实现

### 2.2 当前已经收口到的现实

当前已经确认：

- `<现实1>`
- `<现实2>`
- `<现实3>`

这里要特别避免误判：

- `<常见误判1>`
- `<常见误判2>`

### 2.3 当前仍在使用的存量入口

当前代码里还真实存在下面这批全局或存量入口：

- `<存量入口1>`
- `<存量入口2>`

理解方式必须固定为：

- 它们现在真实存在
- 但它们不自动等于新主线已经完成重组

---

## 3. 当前明确未实现的能力

下面这些方向当前仍未进入实现态：

- `<未实现能力1>`
- `<未实现能力2>`
- `<未实现能力3>`

这些内容只能写成未来路径，不能写成现状能力。

---

## 4. 当前硬边界

- `<硬边界1>`
- `<硬边界2>`
- `<硬边界3>`

---

## 5. 当前校验方式

接手时至少重跑这些命令：

```powershell
git branch --show-current
git rev-parse --short HEAD
git status --short
```

再做最小路径校验：

```powershell
Test-Path <关键文件或目录1>
Test-Path <关键文件或目录2>
Test-Path <关键文件或目录3>
```

推荐理解方式：

- `<校验点1的含义>`
- `<校验点2的含义>`

---

## 6. 更新触发条件

- 真实主路径变化时更新本文件
- 已实现/未实现边界变化时更新本文件
- 现状事实写不下时，优先下沉到子系统实现事实文档，不把本文件写成长流水账

---

## 7. 当前一句话结论

当前 `<项目名>` 已经具备 `<已实现主线>`，但 `<关键缺口>` 仍然没有进入实现态，不能写成现状能力。
