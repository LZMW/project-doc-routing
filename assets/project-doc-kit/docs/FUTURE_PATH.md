# <项目名> 未来路径

> 最后更新：<YYYY-MM-DD>
> 用途：记录 `<项目名>` 的后续主线、推进顺序与硬边界。
> 只回答：后面按什么顺序做、哪些方向已锁、哪些边界不能碰。
> 不负责：当前现状快照、过程流水。

---

## 1. 总体方向

后续主线统一收成：

1. `<主线A>`
2. `<主线B>`
3. `<主线C>`

---

## 2. 未来推进顺序

### 第一阶段

- `<第一阶段项1>`
- `<第一阶段项2>`

### 第二阶段

- `<第二阶段项1>`
- `<第二阶段项2>`

### 后置阶段

- `<后置项1>`
- `<后置项2>`

---

## 3. 硬边界

- 不要 `<禁区1>`
- 不要 `<禁区2>`
- `<真源/主入口>` 仍是 `<固定角色>`

---

## 4. 当前版本与远期边界

当前版本只做到：

- `<版本内目标1>`
- `<版本内目标2>`

远期方向包括但不限于：

- `<远期方向1>`
- `<远期方向2>`

这些远期方向不能回写成现状。

---

## 5. 与任务链的关系

- 这里负责写总顺序与边界
- 具体拆解统一下沉到 `docs/task-chains/*`

---

## 6. 更新触发条件

- 未来顺序变化时更新本文件
- 硬边界变化时更新本文件
- 如果只是单次讨论或未确认想法，不直接写进本文件
