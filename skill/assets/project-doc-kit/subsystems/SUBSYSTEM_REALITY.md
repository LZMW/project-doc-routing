# <子系统名> 实现事实

> 角色：`<子系统名>` 当前实现事实文档
> 最后更新：<YYYY-MM-DD>
> 不负责：阶段结论、未来路线、过程流水

## 1. 阅读顺序

开始任何 `<子系统名>` 工作前，先看：

1. `../docs/INDEX.md`
2. `../docs/CURRENT_STATE.md`
3. `../docs/STAGE_DECISIONS.md`
4. `../docs/FUTURE_PATH.md`
5. 本文件

## 2. 当前真实现状

### 2.1 当前真实主路径

当前真实存在：

- `<路径/对象/主流程1>`
- `<路径/对象/主流程2>`

这说明：

- `<现实判断1>`
- `<现实判断2>`

### 2.2 当前真实未实现

下面这些能力当前仍未进入实现态：

- `<未实现1>`
- `<未实现2>`

如果讨论涉及这些内容，只能按未来路径理解。

## 3. 当前必须守住的边界

- `<边界1>`
- `<边界2>`

## 4. 当前最小校验清单

接手时建议至少跑：

```powershell
git branch --show-current
git rev-parse --short HEAD
git status --short
Test-Path <关键路径1>
Test-Path <关键路径2>
```

## 5. 更新触发条件

- 主路径变化时更新本文件
- 已实现/未实现边界变化时更新本文件
- 过程记录不要写入本文件，统一放到 `../.worklog/CURRENT.txt`

## 6. 当前一句话结论

当前 `<子系统名>` 已具备 `<已实现能力>`，但 `<关键缺口>` 仍未进入实现态。
