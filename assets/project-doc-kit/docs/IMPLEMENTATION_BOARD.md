# <项目名> 当前版本实施总表

> 最后更新：<YYYY-MM-DD>
> 用途：定义当前版本的实施范围、交付目标和完成标准。
> 只回答：这一版做什么、不做什么、做到什么算交付。

---

## 1. 当前版本目标

当前版本目标统一收成：

- `<目标1>`
- `<目标2>`
- `<目标3>`

---

## 2. 当前版本范围

### 在范围内

- `<范围内事项1>`
- `<范围内事项2>`
- `<范围内事项3>`

### 不在范围内

- `<范围外事项1>`
- `<范围外事项2>`

---

## 3. 交付标准

当前版本完成至少意味着：

- `<交付标准1>`
- `<交付标准2>`
- `<交付标准3>`

---

## 4. 工作流分组

| 工作流 | 目标 | 状态 | 备注 |
|---|---|---|---|
| `<工作流1>` | `<目标>` | `<未开始/进行中/完成>` | `<备注>` |
| `<工作流2>` | `<目标>` | `<未开始/进行中/完成>` | `<备注>` |

---

## 5. 当前不做的事

- `<这一版明确不做1>`
- `<这一版明确不做2>`

---

## 6. 校验方式

这一版交付前至少验证：

- `<验证项1>`
- `<验证项2>`
- `<验证项3>`
